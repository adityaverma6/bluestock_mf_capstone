# SQL

- `schema.sql` defines the canonical SQLite tables, constraints, and indexes.
- `queries.sql` contains dashboard-oriented analytical queries.

Run both through the master pipeline or use `scripts/run_schema.py` and
`scripts/run_queries.py` independently.
