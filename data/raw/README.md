# Raw Data

Source CSV extracts used by the ingestion and cleaning notebooks.

Files prefixed `01_` through `10_` are the core capstone datasets. NAV files
named after individual funds are optional outputs from `live_nav_fetch.py`.
Avoid editing source extracts in place; apply transformations in the notebooks.
