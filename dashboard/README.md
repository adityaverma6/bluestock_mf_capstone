# Dashboard

Contains the Power BI dashboard source file:

- `bluestok_mf_dashboard.pbix`

The dashboard consumes the prepared mutual-fund datasets and SQLite analytics.
Exported dashboard pages are stored in `reports/`.
